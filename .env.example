# Quote form submissions use Netlify Forms (no env keys). After deploy, open
# Netlify → Site configuration → Forms to view entries and set email/Slack notifications.
# Local testing: run `netlify dev` so POST / is handled; plain `npm run dev` will not accept form posts.
